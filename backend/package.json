{
  "name": "ballot-builder-api",
  "version": "0.1.0",
  "description": "Ballot Builder Express API Backend",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --watch src src/index.js",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "keywords": ["civic-tech", "voting", "express", "api"],
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
